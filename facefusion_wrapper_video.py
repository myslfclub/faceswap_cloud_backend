def swap_video_faces(video_path, face_path, output_path, resolution):
    # Simule le traitement (remplace ceci par l'appel à FaceFusion réel)
    import shutil
    shutil.copy(video_path, output_path)
